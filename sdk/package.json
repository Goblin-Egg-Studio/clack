{
  "name": "clack-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clack Chat SDK for client applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest"
  }
}
